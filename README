= Dictionary
A simple dictionary app

== Setup
1. `npm install`
2. `rake server`

If that last part doesn't work, try installing 'nodemon' globally:
`npm install -g nodemon`

== Routes:
- /hello
- /define?word=banana
- /add?word=things&definition=just+stuff
- /add?word=things&definition=just+stuff+and+more+stuff&overwrite=1
- /remove?word=things

== Tests:
- `rake nyan`
- `rake test`
- `rake coverage`

Might have to install 'istanbul' globally to get the coverage to work:
`npm install -g istanbul`
